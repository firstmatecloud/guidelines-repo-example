# Adding a cluster on request

## Trigger

When a developer creates a ticket and asks for something like:
* Add a new cluster to the firstmate project
* Create a new aks resource in the firstmate project


## PR details

* PR should be created on the "test-infra-terraform" repo.
* Add another cluster resource in the modules/aks/main.tf file.